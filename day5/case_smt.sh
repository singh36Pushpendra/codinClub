echo "Enter any name: "
read name

case $name in
    Pushpendra)
        echo "$name is from M.P (Dewas)"
        ;;
    Ankit)
        echo "$name is from M.P (Tikamgarh)"
        ;;
    Manish)
        echo "$name is from M.H (Pune)"
        ;;
    *)
        echo "Invalid Input"
esac
